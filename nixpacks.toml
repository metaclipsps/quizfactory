[phases.setup]
aptPkgs = ["ffmpeg", "fonts-dejavu-core", "fonts-liberation"]

[phases.install]
cmds = ["pip install --upgrade pip", "pip install -r requirements.txt"]

[start]
cmd = "python telegram_bot.py"
